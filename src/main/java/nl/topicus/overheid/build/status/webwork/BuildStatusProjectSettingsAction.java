package nl.topicus.overheid.build.status.webwork;

import com.atlassian.jira.web.action.ProjectActionSupport;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class BuildStatusProjectSettingsAction extends ProjectActionSupport {
    private static final Logger log = LoggerFactory.getLogger(BuildStatusProjectSettingsAction.class);

    @Override
    protected String doExecute() throws Exception {
        return super.doExecute();
    }
}
